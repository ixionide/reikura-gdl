name: Rust

env:
  CARGO_TERM_COLOR: always

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  code-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-gnu, x86_64-unknown-linux-gnu
          components: rustfmt, clippy
      - name: Install cpal dependencies
        run: |
          sudo apt-get update
          sudo apt-get install libasound2-dev
      - name: Check
        run: |
          cargo fmt --check
          cargo clippy --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace

  build-win:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: i686-pc-windows-gnu,i686-pc-windows-msvc,x86_64-pc-windows-gnu,x86_64-pc-windows-msvc
      - name: Build
        run: cargo build --all-targets

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: i686-unknown-linux-gnu,i686-unknown-linux-musl,x86_64-unknown-linux-gnu,x86_64-unknown-linux-musl,x86_64-linux-android
      - name: Install cpal dependencies
        run: |
          sudo apt-get update
          sudo apt-get install libasound2-dev
      - name: Build
        run: cargo build --all-targets
